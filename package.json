{
  "name": "tomato-root",
  "version": "1.0.0",
  "description": "Tomato MERN Stack Food Delivery website local launcher",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin",
    "backend": "npm run server --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "admin": "npm run dev --prefix admin",
    "start": "concurrently \"npm run backend\" \"npm run frontend\" \"npm run admin\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
